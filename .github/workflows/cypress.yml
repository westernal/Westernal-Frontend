name: Cypress Tests

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set environment variables
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: |
         if [ "$TOKEN" == "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiI2NDE2ZDRkYWI3Mjg4MGE0MjFiYjZjYzgiLCJ1c2VybmFtZSI6Im5ld3Rlc3R1c2VyIiwiaWF0IjoxNjg4NjQyOTAyfQ.TVydoU8KZDgziAOTkbiZxypuULektqUq4OKK8661ei4" ]
          then
            echo "Got secret"
          fi
      - name: Cypress run
        uses: cypress-io/github-action@v5.1.0
        with:
          build: npm run build
          start: npm start
